<Window x:Class="FileDownloader.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="150" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageTextBlock" Margin="0,0,0,10" TextWrapping="Wrap"/>
        <TextBox Grid.Row="1" x:Name="AnswerTextBox" 
                 Margin="0,0,0,15" KeyDown="AnswerTextBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Margin="5" Padding="20,8"/>
            <Button Content="Отмена" IsCancel="True" Margin="5" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>

